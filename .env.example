DEBUG=True
# Generate a production key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=change-me-to-a-long-random-string-in-production

USE_SQLITE=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (set USE_SQLITE=False and fill these)
DB_NAME=poytakht_crm
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Production (Railway sets DATABASE_URL automatically)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
