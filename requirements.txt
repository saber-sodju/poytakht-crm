Django>=5.2,<5.3
gunicorn>=21.0
psycopg2-binary>=2.9
python-dotenv>=1.0
Pillow>=10.0
django-crispy-forms>=2.0
crispy-bootstrap5>=0.7
django-filter>=23.0
whitenoise>=6.6
openpyxl>=3.1
reportlab>=4.0
django-extensions>=3.2

# Optional in dev, becomes active only when the matching env vars are set —
# see README_PRODUCTION.md "S3 media storage" and "Error monitoring".
django-storages>=1.14
boto3>=1.34
sentry-sdk>=2.0
